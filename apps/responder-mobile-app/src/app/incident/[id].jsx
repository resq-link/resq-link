import CaseDetailView from "@/modules/incidents/components/CaseDetailView";

/** Stack screen: `/incident/:id` — detail only, not a main tab. */
export default function IncidentDetailPage() {
  return <CaseDetailView />;
}
