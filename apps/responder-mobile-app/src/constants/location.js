/**
 * App-wide AsyncStorage key: when `"true"`, dispatcher location sharing is paused.
 * Used by dashboard, map presence, settings, and the location support screen — keep in `constants/`.
 */
export const LOCATION_PAUSED_KEY = "responder_location_paused";
