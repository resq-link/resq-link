'use client';

/**
 * Legacy path — Edit Account now uses a modal (StaffEditDialog), matching Agencies.
 * Re-exported so any old imports keep working.
 */
export { StaffEditDialog, StaffEditDialog as StaffEditDrawer } from './StaffEditDialog';
