{
  "private": true,
  "name": "tuguegarao-rescue-system",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:firebase": "npm run build --prefix packages/firebase"
  },
  "dependencies": {
    "@expo/ngrok": "^4.1.3"
  },
  "overrides": {
    "react": "19.0.0",
    "react-dom": "19.0.0"
  }
}
