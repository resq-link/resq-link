diff --git a/node_modules/sonner-native/lib/commonjs/positioner.js b/node_modules/sonner-native/lib/commonjs/positioner.js
index 63b14fd..45501b6 100644
--- a/node_modules/sonner-native/lib/commonjs/positioner.js
+++ b/node_modules/sonner-native/lib/commonjs/positioner.js
@@ -55,9 +55,14 @@ const Positioner = ({
     return {};
   }, [position, bottom, top, offset]);
   const hasChildren = _react.default.Children.count(children) > 0;
+  const pointerEvents = _reactNative.Platform.OS === 'android' && !hasChildren ? 'none' : 'box-none';
   return /*#__PURE__*/(0, _jsxRuntime.jsx)(_reactNative.View, {
+    ...(_reactNative.Platform.OS === 'web' ? {
+    style: [{ pointerEvents }, containerStyle, insetValues, style]
+    } : {
     style: [containerStyle, insetValues, style],
-    pointerEvents: _reactNative.Platform.OS === 'android' && !hasChildren ? 'none' : 'box-none',
+    pointerEvents
+    }),
     ...props,
     children: children
   });
diff --git a/node_modules/sonner-native/lib/module/positioner.js b/node_modules/sonner-native/lib/module/positioner.js
index 4421af7..9b3fa11 100644
--- a/node_modules/sonner-native/lib/module/positioner.js
+++ b/node_modules/sonner-native/lib/module/positioner.js
@@ -50,9 +50,14 @@ export const Positioner = ({
     return {};
   }, [position, bottom, top, offset]);
   const hasChildren = React.Children.count(children) > 0;
+  const pointerEvents = Platform.OS === 'android' && !hasChildren ? 'none' : 'box-none';
   return /*#__PURE__*/_jsx(View, {
+    ...(Platform.OS === 'web' ? {
+    style: [{ pointerEvents }, containerStyle, insetValues, style]
+    } : {
     style: [containerStyle, insetValues, style],
-    pointerEvents: Platform.OS === 'android' && !hasChildren ? 'none' : 'box-none',
+    pointerEvents
+    }),
     ...props,
     children: children
   });
