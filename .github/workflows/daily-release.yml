name: Reset staging to main (nightly)

on:
  workflow_dispatch:

# Disabled until CI_PUSH_TOKEN is configured and staging/release branches exist.
# Previously failed with: Input required and not supplied: token
# jobs:
#   cut-staging-release:
#     if: ${{ secrets.CI_PUSH_TOKEN != '' }}
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           token: ${{ secrets.CI_PUSH_TOKEN }}
#           fetch-depth: 0
#       ...
