{
  "name": "resq-link-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions for RESQ-Link — responder assignment push alerts",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && cross-env FUNCTIONS_DISCOVERY_TIMEOUT=120 firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "typescript": "^5.9.3"
  }
}
